# 예상원가 자동 계산 기능

## ✅ 기능 개요

제품정보 영역의 **예상원가**가 입력 필드에서 **자동 계산 표시**로 변경되었습니다.

## 💡 예상 단가 계산 공식

```
예상 단가 = (최종 금액 / 수량) + (41/1000 × 1개 무게) + (4.8/(5000/1개 무게)) + (1/(5000/1개 무게))
```

**최종 금액 구성:**
```
최종 금액 = 총계 + 수수료 + 공장배송비 + 추가비용
         = (단가 × 수량) + 수수료 + 공장배송비 + 추가비용
```

**단순화된 표현:**
```
예상 단가 = (최종금액/수량) + (0.041 × 무게) + (4.8 × 무게 / 5000) + (무게 / 5000)
```

**데이터 소스:**
- `project.unit_price` (단가) - 비용정보 탭에서 입력 (¥)
- `project.quantity` (수량) - 프로젝트 기본 정보
- `project.fee` (수수료) - 비용정보 탭에서 계산 (¥)
- `project.factory_shipping_cost` (공장배송비) - 비용정보 탭에서 입력 (¥)
- `project.additional_cost_items` (추가비용) - 비용정보 탭에서 입력 (배열)
- `project.unit_weight` (1개 무게) - 제품정보에서 입력 (g)

## 📊 UI 변경사항

### 변경 전 (입력 필드)
```
┌──────────────┐
│ 예상원가     │
│ [500] ¥     │ ← 입력 필드
└──────────────┘
```

### 변경 후 (자동 계산)
```
┌────────────────────────────┐
│ 예상원가                   │
│ ¥525.58 (자동)            │ ← 읽기 전용
│ 100g 기준                  │ ← 무게 표시
└────────────────────────────┘
```

**특징:**
- ✅ 읽기 전용 (입력 불가)
- ✅ 소수점 2자리까지 표시
- ✅ 청록색으로 강조 표시
- ✅ "(자동)" 표시로 자동 계산임을 명확화
- ✅ 무게 기준 정보 표시 (예: "100g 기준")
- ✅ 마우스 오버 시 툴팁: "단가×(1+수수료율) + 무게기반 비용"

## 💻 코드 구현

### 클라이언트 측 (ProdInfo.js)

```javascript
// 예상 단가 자동 계산 함수
const calculateEstimatedCost = () => {
  const quantity = Number(project.quantity) || 0;
  const unitWeight = Number(project.unit_weight) || 0;
  
  if (quantity === 0) {
    return 0;
  }
  
  // 최종 금액 계산: 총계 + 수수료 + 공장배송비 + 추가비용
  const subtotal = Number(project.unit_price) * quantity || 0;
  const fee = Number(project.fee) || 0;
  const factoryShippingCost = Number(project.factory_shipping_cost) || 0;
  
  // 추가비용 합계
  let additionalCostTotal = 0;
  if (project.additional_cost_items) {
    const additionalCosts = typeof project.additional_cost_items === 'string'
      ? JSON.parse(project.additional_cost_items)
      : project.additional_cost_items;
    
    if (Array.isArray(additionalCosts)) {
      additionalCostTotal = additionalCosts.reduce((sum, item) => 
        sum + (Number(item.cost) || 0), 0);
    }
  }
  
  const finalAmount = subtotal + fee + factoryShippingCost + additionalCostTotal;
  
  // 1. 최종 금액 / 수량
  const pricePerUnit = finalAmount / quantity;
  
  // 2. 41/1000 × 1개 무게
  const weightCost1 = (41 / 1000) * unitWeight;
  
  // 3. 4.8/(5000/1개 무게)
  const weightCost2 = unitWeight > 0 ? (4.8 / (5000 / unitWeight)) : 0;
  
  // 4. 1/(5000/1개 무게)
  const weightCost3 = unitWeight > 0 ? (1 / (5000 / unitWeight)) : 0;
  
  // 총 예상 단가
  return pricePerUnit + weightCost1 + weightCost2 + weightCost3;
};

// UI 렌더링
<div className="flex flex-col space-y-1">
  <div className="flex items-center space-x-2">
    <span className="text-sm font-semibold text-teal-700" 
          title="(최종금액/수량) + 무게기반 비용">
      {calculateEstimatedCost() > 0 
        ? `¥${calculateEstimatedCost().toFixed(2)}` 
        : '-'}
    </span>
    <span className="text-xs text-gray-500">(자동)</span>
  </div>
  {project.unit_weight > 0 && (
    <span className="text-xs text-gray-400">
      {project.unit_weight}g 기준
    </span>
  )}
</div>
```

## 📋 제품정보 필드 구성 (최종)

### 첫 번째 행 (입력 가능)
1. **1개 무게** - 숫자 입력 (g)
2. **제품사이즈** - 텍스트 입력 (cm)
3. **소포장 방식** - 텍스트 입력
4. **한박스 입수량** - 텍스트 입력 (개)

### 두 번째 행 (입력 가능 + 자동 계산)
1. **공급자 이름** - 텍스트 입력
2. **공장 납기소요일** - 숫자 입력 (일)
3. **예상원가** - 🔒 자동 계산 (예상 단가)
4. **-** - 예비 필드

## 🔄 계산 로직

### 예시 1: 기본 계산
```javascript
단가 (unit_price): ¥500
수량 (quantity): 100개
수수료 (fee): ¥2,500
공장배송비 (factory_shipping_cost): ¥1,000
추가비용: ¥0
1개 무게 (unit_weight): 100g
────────────────────────────────
총계 = 500 × 100 = ¥50,000
최종 금액 = 50,000 + 2,500 + 1,000 + 0 = ¥53,500
예상 단가: ¥539.22
```

**계산 과정:**
```
1. 최종 금액 / 수량 = 53,500 / 100 = 535.00
2. 41/1000 × 무게 = 0.041 × 100 = 4.10
3. 4.8/(5000/무게) = 4.8/(5000/100) = 4.8/50 = 0.096
4. 1/(5000/무게) = 1/(5000/100) = 1/50 = 0.02
5. 합계 = 535.00 + 4.10 + 0.096 + 0.02 = 539.216 ≈ ¥539.22
```

### 예시 2: 무게가 무거운 경우
```javascript
단가 (unit_price): ¥1,000
수량 (quantity): 100개
수수료 (fee): ¥5,000
공장배송비 (factory_shipping_cost): ¥2,000
추가비용: ¥1,000
1개 무게 (unit_weight): 500g
────────────────────────────────
총계 = 1,000 × 100 = ¥100,000
최종 금액 = 100,000 + 5,000 + 2,000 + 1,000 = ¥108,000
예상 단가: ¥1,101.08
```

**계산 과정:**
```
1. 최종 금액 / 수량 = 108,000 / 100 = 1,080.00
2. 41/1000 × 무게 = 0.041 × 500 = 20.50
3. 4.8/(5000/무게) = 4.8 × 500/5000 = 0.48
4. 1/(5000/무게) = 500/5000 = 0.10
5. 합계 = 1,080.00 + 20.50 + 0.48 + 0.10 = ¥1,101.08
```

### 예시 3: 무게가 가벼운 경우
```javascript
단가 (unit_price): ¥300
수량 (quantity): 200개
수수료 (fee): ¥900
공장배송비 (factory_shipping_cost): ¥500
추가비용: ¥0
1개 무게 (unit_weight): 20g
────────────────────────────────
총계 = 300 × 200 = ¥60,000
최종 금액 = 60,000 + 900 + 500 + 0 = ¥61,400
예상 단가: ¥307.84
```

**계산 과정:**
```
1. 최종 금액 / 수량 = 61,400 / 200 = 307.00
2. 41/1000 × 무게 = 0.041 × 20 = 0.82
3. 4.8/(5000/무게) = 4.8 × 20/5000 = 0.0192
4. 1/(5000/무게) = 20/5000 = 0.004
5. 합계 = 307.00 + 0.82 + 0.0192 + 0.004 ≈ ¥307.84
```

### 예시 4: 값이 없는 경우
```javascript
단가: 없음 (0)
수수료율: 없음 (0)
1개 무게: 없음 (0)
────────────────────────
예상원가: - (표시 안 함)
```

### 예시 5: 실시간 반영
- 비용정보 탭에서 단가나 수수료율 변경
- 제품정보에서 1개 무게 변경
- 예상원가 자동 업데이트 ✨

## 📐 공식 구성 요소 설명

### 1. 최종 금액 / 수량
- **목적**: 모든 비용을 포함한 1개당 기본 가격
- **계산**: (총계 + 수수료 + 공장배송비 + 추가비용) / 수량
- **예시**: ¥53,500 / 100개 = ¥535.00

**최종 금액 구성:**
```
총계 = 단가 × 수량 = 500 × 100 = ¥50,000
수수료 = ¥2,500
공장배송비 = ¥1,000
추가비용 = ¥0
─────────────────────────
최종 금액 = ¥53,500
```

### 2. 41/1000 × 1개 무게
- **목적**: 무게당 고정 비용 (무게 비례)
- **계수**: 0.041 (41/1000)
- **예시**: 0.041 × 100g = ¥4.10

### 3. 4.8/(5000/1개 무게)
- **목적**: 무게 기반 변동 비용 1
- **단순화**: 4.8 × 무게 / 5000
- **예시**: 4.8 × 100 / 5000 = ¥0.096

### 4. 1/(5000/1개 무게)
- **목적**: 무게 기반 변동 비용 2
- **단순화**: 무게 / 5000
- **예시**: 100 / 5000 = ¥0.02

### 총합
```
535.00 + 4.10 + 0.096 + 0.02 = ¥539.216 → ¥539.22 (소수점 2자리)
```

## 📡 데이터 흐름

```
비용정보 탭
├─ unit_price (단가 입력) ← 사용자 입력
├─ fee (수수료) ← 자동 계산
├─ factory_shipping_cost (공장배송비) ← 사용자 입력
├─ additional_cost_items (추가비용) ← 사용자 입력
└─ 저장 → DB 저장

제품정보
├─ unit_weight (1개 무게 입력) ← 사용자 입력
└─ 저장 → DB 저장
     │
     ↓
기본정보 탭 (ProdInfo)
     │
     ↓
최종금액 = (단가×수량) + 수수료 + 공장배송비 + 추가비용
     │
     ↓
예상 단가 = (최종금액/수량) 
          + (41/1000 × 무게) 
          + (4.8/(5000/무게)) 
          + (1/(5000/무게))
     │
     ↓
  자동 표시
```

**데이터 사용:**
- `unit_price`: 비용정보 탭에서 입력
- `quantity`: 프로젝트 기본 정보
- `fee`: 비용정보 탭에서 계산
- `factory_shipping_cost`: 비용정보 탭에서 입력
- `additional_cost_items`: 비용정보 탭에서 입력
- `unit_weight`: 제품정보에서 입력 (같은 화면)
- 계산은 클라이언트 측에서 실시간 수행

## 🎯 사용자 경험

### 이전 방식
- ❌ 예상원가를 수동으로 입력해야 함
- ❌ 단가나 수수료 변경 시 예상원가도 수동 수정
- ❌ 계산 오류 가능성

### 새로운 방식
- ✅ 예상원가 자동 계산
- ✅ 단가/수수료 변경 시 자동 반영
- ✅ 계산 오류 없음
- ✅ 사용자 편의성 향상

## 🔧 제거된 기능

- ❌ `estimated_cost` 데이터베이스 필드 (마이그레이션에서 제거)
- ❌ 예상원가 입력 필드
- ❌ 예상원가 State 관리
- ❌ 예상원가 저장 로직

## 📊 코드 변경 요약

| 파일 | 변경사항 |
|------|---------|
| `client/src/components/Project/MJ/Details/ProdInfo.js` | 입력 필드 → 자동 계산 표시 |
| `server/routes/mj-project.js` | `allowedFields`에서 `estimated_cost` 제거 |
| `server/config/database.js` | 마이그레이션에서 `estimated_cost` 제거 |
| `server/migrations/add_product_info_fields.sql` | `estimated_cost` 필드 제거 |
| `server/run-product-info-migration.js` | `estimated_cost` 필드 제거 |

## 🎯 사용 예시

### 케이스 1: 일반적인 제품
```
입력값:
- 단가: ¥500
- 수수료율: 5%
- 1개 무게: 100g

계산:
1. 단가 × (1 + 수수료율) = 500 × 1.05 = 525.00
2. 41/1000 × 무게 = 0.041 × 100 = 4.10
3. 4.8/(5000/무게) = 4.8 × 100 / 5000 = 0.096
4. 1/(5000/무게) = 100 / 5000 = 0.02

결과:
- 예상 단가: ¥529.22
- 총 예상비용 (100개): ¥52,922
```

### 케이스 2: 무거운 제품
```
입력값:
- 단가: ¥1,000
- 수수료율: 10%
- 1개 무게: 500g

계산:
1. 단가 × (1 + 수수료율) = 1,000 × 1.10 = 1,100.00
2. 41/1000 × 무게 = 0.041 × 500 = 20.50
3. 4.8/(5000/무게) = 4.8 × 500 / 5000 = 0.48
4. 1/(5000/무게) = 500 / 5000 = 0.10

결과:
- 예상 단가: ¥1,121.08
- 총 예상비용 (100개): ¥112,108
```

### 케이스 3: 가벼운 제품
```
입력값:
- 단가: ¥300
- 수수료율: 3%
- 1개 무게: 20g

계산:
1. 단가 × (1 + 수수료율) = 300 × 1.03 = 309.00
2. 41/1000 × 무게 = 0.041 × 20 = 0.82
3. 4.8/(5000/무게) = 4.8 × 20 / 5000 = 0.0192
4. 1/(5000/무게) = 20 / 5000 = 0.004

결과:
- 예상 단가: ¥309.84
- 총 예상비용 (100개): ¥30,984
```

## 💡 향후 활용

예상원가 공식을 다음과 같이 확장 가능:
- 공장배송비 포함: `단가 × (1 + 수수료율) + 공장배송비/수량`
- 환율 적용: `[단가 × (1 + 수수료율)] × 환율`
- 추가비용 포함: `단가 × (1 + 수수료율) + 추가비용/수량`

현재는 **단가 × (1 + 수수료율)**로 계산합니다.

---

**작성일**: 2025-01-10  
**기능 상태**: ✅ 완료  
**계산 공식**: 단가 × (1 + 수수료율)  
**타입**: 자동 계산 표시 (읽기 전용)  
**서버 재시작**: 불필요 (클라이언트 측 변경만)

