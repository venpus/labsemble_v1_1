# 📅 주문 달력 UI 개선 및 이벤트 표시 로직 수정

## ✅ 개요

모바일 앱의 주문 달력 화면에서 다음 두 가지 주요 개선 작업을 수행했습니다:
1. **캘린더 높이 축소 및 이벤트 영역 확대** - 이벤트 내용을 더 잘 볼 수 있도록 UI 개선
2. **이벤트 표시 날짜 로직 수정** - 각 이벤트가 올바른 날짜에 표시되도록 수정

## 🔄 변경 사항

### 1. UI 개선 - 캘린더 높이 축소 및 이벤트 영역 확대

#### 변경 전 문제점
- 캘린더가 화면의 많은 공간을 차지함
- 이벤트 목록 영역이 좁아서 내용을 보기 불편함
- 패딩과 폰트 크기가 과도하게 커서 공간 낭비

#### 변경 후 개선 사항

**캘린더 영역:**
- 달력 셀 높이: `aspectRatio(1f)` → `40.dp` 고정 높이
- 카드 패딩: `16.dp` → `12.dp`
- 카드 elevation: `4.dp` → `2.dp`
- 월 제목 폰트: `18.sp` → `16.sp`
- 요일 헤더 폰트: `14.sp` → `12.sp`
- 날짜 숫자 폰트: `12.sp` → `11.sp`
- 이벤트 인디케이터 크기: `3.dp` → `4.dp`
- 이벤트 인디케이터 배치: 세로 → 가로

**이벤트 영역:**
- 제목 포맷: "선택한 날짜: yyyy년 MM월 dd일" → "MM월 dd일 이벤트"
- 제목 폰트: `16.sp` → `15.sp`
- 그룹 헤더 패딩: `12dp, 8dp` → `10dp, 6dp`
- 그룹 헤더 폰트: `16.sp` → `14.sp`
- 아이템 카드 패딩: `12.dp` → `10.dp`
- 아이템 카드 elevation: `2.dp` → `1.dp`
- 아이템 폰트: `14.sp/12.sp` → `13.sp/11.sp`
- 텍스트에 `maxLines = 1` 추가하여 줄바꿈 방지

### 2. 이벤트 표시 날짜 로직 수정

#### 변경 전 문제점
모든 이벤트가 `event.date` (발주일)를 기준으로 표시되어, 발주일에 클릭하면 발주 완료, 납기 예정일, 공장 출고 완료 이벤트가 모두 함께 표시됨.

#### 변경 후 로직

각 이벤트 타입이 올바른 날짜에 표시됨:

| 이벤트 타입 | 표시 기준 날짜 | 조건 |
|------------|--------------|------|
| 🟢 발주 완료 | `event.date` (발주일) | `isOrderCompleted == true` |
| 🔵 납기 예정일 | `event.expectedDeliveryDate` (납기 예정일) | `isOrderCompleted == true` && `actualFactoryShippingDate == null` |
| 🟢 공장 출고 완료 | `event.actualFactoryShippingDate` (실제 출고일) | `actualFactoryShippingDate != null` |

#### 코드 변경 위치

**1. `OrderCalendarCell` - 달력 셀의 이벤트 인디케이터:**

```kotlin
// 발주완료 이벤트 (발주일 기준)
val orderEvents = events.filter { event ->
    try {
        event.isOrderCompleted && LocalDate.parse(event.date) == date
    } catch (e: Exception) {
        false
    }
}

// 납기예정일 이벤트 (납기 예정일 기준, 공장출고 안된 경우)
val deliveryEvents = events.filter { event ->
    try {
        event.isOrderCompleted && 
        event.actualFactoryShippingDate == null && 
        LocalDate.parse(event.expectedDeliveryDate) == date
    } catch (e: Exception) {
        false
    }
}

// 공장출고완료 이벤트 (실제 출고일 기준)
val shippedEvents = events.filter { event ->
    try {
        event.actualFactoryShippingDate != null && 
        LocalDate.parse(event.actualFactoryShippingDate) == date
    } catch (e: Exception) {
        false
    }
}
```

**2. `SelectedDateOrderEvents` - 선택한 날짜의 이벤트 목록:**

동일한 필터링 로직을 적용하여 각 이벤트가 올바른 날짜에만 표시되도록 수정.

## 📱 사용자 경험 개선

### Before (변경 전)
```
┌─────────────────────────────────────┐
│         2025년 01월                 │
│  (큰 캘린더 - 화면의 70% 차지)      │
│                                     │
│    [큰 셀들]                        │
│                                     │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 1월 15일 (발주일) 클릭 시:          │
├─────────────────────────────────────┤
│ 🟢 발주 완료 (2)                    │ ✅ 맞음
│ 🔵 납기 예정일 (2)                  │ ❌ 틀림 - 다른 날짜
│ 🟢 공장 출고 완료 (1)               │ ❌ 틀림 - 다른 날짜
└─────────────────────────────────────┘

    ⬇️ (이벤트 공간 부족)
```

### After (변경 후)
```
┌─────────────────────────────────────┐
│        2025년 01월                  │
│  (컴팩트한 캘린더 - 화면의 40%)     │
│   [작은 셀들]                       │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 1월 15일 (발주일) 클릭 시:          │
├─────────────────────────────────────┤
│ 🟢 발주 완료 (2)                    │ ✅ 맞음
│   • 상품A - 수량: 100개             │
│   • 상품B - 수량: 200개             │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│ 1월 30일 (납기일) 클릭 시:          │
├─────────────────────────────────────┤
│ 🔵 납기 예정일 (2)                  │ ✅ 맞음
│   • 상품A - 납기일: 1/30            │
│   • 상품B - 납기일: 1/30            │
└─────────────────────────────────────┘

    ⬇️ (넓은 이벤트 공간 - 60%)
```

## 🎨 디자인 변경 상세

### 캘린더 카드
- **Padding**: `16.dp` → `12.dp`
- **Elevation**: `4.dp` → `2.dp`
- **월 네비게이션**: 아이콘 크기 `20.dp` 추가
- **간격**: `16.dp` → `8.dp`, `8.dp` → `4.dp`

### 달력 그리드
- **행 높이**: `aspectRatio(1f)` (정사각형) → `40.dp` (고정)
- **셀 패딩**: `2.dp` → `1.dp`
- **RoundedCornerShape**: `8.dp` → `6.dp`

### 이벤트 목록
- **LazyColumn 간격**: `12.dp` → `8.dp`
- **그룹 헤더**:
  - 패딩: `12dp, 8dp` → `10dp, 6dp`
  - 아이콘: `20.dp` → `16.dp`
  - 폰트: `16.sp` → `14.sp`
  - RoundedCornerShape: `8.dp` → `6.dp`

### 이벤트 아이템
- **카드**:
  - Elevation: `2.dp` → `1.dp`
  - RoundedCornerShape: `8.dp` → `6.dp`
  - 패딩: `12.dp` → `10.dp`
- **아이콘**:
  - 크기: `32.dp` → `28.dp`
  - 내부 아이콘: `16.dp` → `14.dp`
- **텍스트**:
  - 제목: `14.sp` → `13.sp`, `maxLines = 1`
  - 설명: `12.sp` → `11.sp`
  - 간격: `12.dp` → `10.dp`, `2.dp` → `1.dp`

## 📊 공간 효율성

### 변경 전
- 캘린더 영역: ~70% (고정)
- 이벤트 영역: ~30% (스크롤 필요)

### 변경 후
- 캘린더 영역: ~40% (축소)
- 이벤트 영역: ~60% (확대)
- **결과**: 이벤트 영역이 2배 증가하여 더 많은 정보를 한 화면에 표시 가능

## 🔍 이벤트 표시 예시

### 시나리오: 상품 A 발주

1. **1월 5일 (발주일)**
   - 🟢 발주 완료: 상품A (수량: 1000개)

2. **1월 25일 (납기 예정일 = 발주일 + 20일)**
   - 🔵 납기 예정일: 상품A

3. **1월 23일 (실제 공장 출고일 - 2일 일찍 출고)**
   - 🟢 공장 출고 완료: 상품A

이제 각 날짜에 해당하는 이벤트만 정확하게 표시됩니다!

## 📁 수정된 파일

### 주요 파일
- `AndroidAPP/app/src/main/java/com/example/myapplication/ui/calendar/OrderCalendarScreen.kt`

### 변경 함수
1. `OrderCalendarScreen` - 레이아웃 구조 개선
2. `OrderCalendarView` - 캘린더 카드 스타일 압축
3. `OrderCalendarCell` - 셀 높이 축소 및 이벤트 필터링 로직 수정
4. `SelectedDateOrderEvents` - 이벤트 필터링 로직 수정
5. `OrderEventGroupHeader` - 헤더 스타일 압축
6. `OrderEventItem` - 아이템 스타일 압축
7. `DeliveryEventItem` - 아이템 스타일 압축
8. `ShippedEventItem` - 아이템 스타일 압축

## ✅ 테스트 체크리스트

- [x] 캘린더 높이가 줄어들어 더 많은 이벤트 공간 확보
- [x] 발주 완료 이벤트가 발주일에 정확히 표시됨
- [x] 납기 예정일 이벤트가 납기 예정일에 정확히 표시됨
- [x] 공장 출고 완료 이벤트가 실제 출고일에 정확히 표시됨
- [x] 각 날짜에 올바른 이벤트만 표시됨
- [x] 이벤트 인디케이터(점)가 캘린더 셀에 올바르게 표시됨
- [x] 모든 텍스트가 읽기 쉽고 정보가 명확하게 표시됨
- [x] Lint 오류 없음
- [x] 빌드 성공

## 🚀 배포

빌드된 APK 경로:
```
AndroidAPP/app/build/outputs/apk/debug/app-debug.apk
```

## 📝 참고사항

### 날짜 필드 정리

`OrderCalendarEvent` 모델의 날짜 필드:
- `date`: 발주일 (기준 날짜)
- `expectedDeliveryDate`: 납기 예정일
- `actualFactoryShippingDate`: 실제 공장 출고일 (nullable)

### 이벤트 색상 구분

- 🟢 **초록색** (`#4CAF50`): 발주 완료
- 🔵 **파란색** (`#2196F3`): 납기 예정일
- 🟢 **진한 초록색** (`#2E7D32`): 공장 출고 완료

---

**작성일**: 2025-10-10  
**작성자**: AI Assistant  
**버전**: 1.0

